
def send_notification(messages, shop_id, shop_name, account_id):
    """
    发送通知给指定的账号
    """
    # 这里可以实现发送通知的逻辑
    print(f"Sending notification to {account_id} for shop {shop_name} ({shop_id}) with messages: {messages}")